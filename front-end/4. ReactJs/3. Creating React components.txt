File Extensions
.js 
- JavaScript
- Contains regular javascript code
- Used for general logic and Components

.jsx 
- stands for javascript xml
- combines javascript with html like tags
- makes it easier to design UI Components
--------------------------------------------------------------------------------------------------------------
jsx File:
function App(){ // functional component
  return <h1>
    Namskara guru
  </h1>
}

export default App // to re-use this component this export keyword is used

  functional component
 - Initially stateless
 - Can use Hooks for state and effects
 - Can be used in any React Project
 - Simpler and more concise
 - Can be reused
 - Can be used in any React Project
 - More popular

--------------------------------------------------------------------------------------------------------------
What is JSX?
- JSX determines how the UI will look wherever the component is used
- Not HTML: Though it resembles html, you are actually writting JSX, which stands for JavaScript xml
- Conversion: JSX gets converted to regular JavaScript
- BabelJS.io/repl is a tool that allows you to see how JSX is transformed into JavaScript

Exporting Components
export default      multiple named exports          named export and one 
                                                    default export

function()          export function                 export function
{                   Slider() {                      Avatar(){
  ....              ...                             ...
}                   }                               }
                    
                    export function                 export default  
                    Checkbox(){                     function
                    ...                             FriendsList(){
                    }                               ...
                                                    }
1. Enables the use of a component in other parts.
2. Default Export: Allows exportting a single component as the default from a module
3. Named export: Allows exporting multiple items from a module
4. Importing: To use an exported component, you need to import it in the destination file using import syntax. 
--------------------------------------------------------------------------------------------------------------

Steps to create React Application
1. (npm create vite@latest)

Gives option to choose from multiple Js framworks to continue doing devlopment( select the desired framwork, create name for the application, change the directory to that application)
Ex: 
npm create vite@latest
project name: first-react-app
select a framework:React
select a varient: Javascript

2. cd first-react-app

3. npm install : Install required dependacies for react application project

4. npm rundev : runs the server

